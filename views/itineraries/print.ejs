<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title %></title>
  <style>
    @page { margin: 16mm 12mm; }
    :root {
      --primary: #0d6efd;
      --muted: #6c757d;
      --border: #dee2e6;
      --bg: #f8f9fa;
    }
    body { font-family: "Segoe UI", Roboto, Arial, sans-serif; margin: 0; background: #fff; color: #212529; }
    .sheet { max-width: 960px; margin: 0 auto; padding: 2.5rem 2rem 3rem; }
    .header { text-align: center; margin-bottom: 2rem; }
    .header h1 { margin: 0; font-size: 2rem; text-transform: uppercase; letter-spacing: 0.08em; }
    .header p { margin: 0.35rem 0; color: var(--muted); }
    .meta-grid { display: grid; grid-template-columns: repeat(auto-fit,minmax(180px,1fr)); gap: 1rem; margin-bottom: 2rem; }
    .meta-card { border: 1px solid var(--border); border-radius: 0.5rem; padding: 1rem; background: var(--bg); }
    .meta-card span { display: block; font-size: 0.8rem; text-transform: uppercase; color: var(--muted); letter-spacing: 0.05em; }
    .meta-card strong { font-size: 1.1rem; }
    .section { margin-top: 2.5rem; }
    .section h2 { font-size: 1.1rem; text-transform: uppercase; letter-spacing: 0.08em; margin-bottom: 1rem; }
    .day-card { border: 1px solid var(--border); border-radius: 0.75rem; padding: 1.25rem; margin-bottom: 1.25rem; break-inside: avoid; page-break-inside: avoid; }
    .day-card h3 { margin: 0 0 0.5rem; font-size: 1rem; text-transform: uppercase; letter-spacing: 0.05em; }
    .day-card p { margin: 0.5rem 0; line-height: 1.6; }
    .day-card .accommodation { font-weight: 600; }
    .day-grid { display: grid; grid-template-columns: 1fr 260px; gap: 1rem; align-items: start; }
    .day-image img { width: 100%; height: auto; border-radius: 0.5rem; border: 1px solid var(--border); object-fit: cover; }
    .section, .meta-grid, .header { break-inside: avoid; page-break-inside: avoid; }
    h2, h3 { break-after: avoid; page-break-after: avoid; }
    p, ul, li, img { orphans: 3; widows: 3; }
    .list-grid { display: grid; grid-template-columns: repeat(auto-fit,minmax(280px,1fr)); gap: 1.5rem; }
    ul { margin: 0; padding-left: 1.2rem; line-height: 1.6; }
    .price-table { width: 100%; border-collapse: collapse; }
    .price-table th,.price-table td { text-align: left; padding: 0.75rem; border-bottom: 1px solid var(--border); }
    .price-table th { text-transform: uppercase; font-size: 0.85rem; color: var(--muted); letter-spacing: 0.08em; }
    .print-actions { margin-top: 3rem; text-align: center; }
    .print-btn { background: var(--primary); color: #fff; border: none; padding: 0.85rem 2.5rem; border-radius: 999px; font-size: 1rem; cursor: pointer; }
    .company-info { margin-top: 2rem; padding-top: 1rem; border-top: 1px solid var(--border); color: var(--muted); }
    .company-grid { display: grid; grid-template-columns: repeat(auto-fit,minmax(220px,1fr)); gap: 0.75rem 1.5rem; align-items: start; text-align: center; }
    .company-grid a { color: inherit; text-decoration: none; }
    .company-grid a:hover { text-decoration: underline; }
    @media print {
      * {
        -webkit-print-color-adjust: exact;
        print-color-adjust: exact;
      }
      .print-btn { display: none; }
      body { background: #fff; }
      .sheet { padding: 1.5rem 1.25rem; }
    }
  </style>
</head>
<body>
  <div class="sheet">
    <header class="header">
      <img src="/images/logo.png" alt="Company Logo" width="200" height="110" style="display:block;margin:0 auto 0.5rem;" />
      <p class="text-muted">Custom Safari Brief</p>
      <h1><%= it.title %></h1>
      <p>Travel date: <%= it.startDate ? new Date(it.startDate).toLocaleDateString('en-GB') : 'TBD' %></p>
      <p>Accommodation plan: <%= accomodationList.length ? accomodationList.join(', ') : 'To be confirmed' %></p>
    </header>

    <div class="meta-grid">
      <div class="meta-card"><span>Duration</span><strong><%= formattedDays.length %> Days</strong></div>
      <% if (it.clientName) { %>
        <div class="meta-card"><span>Client</span><strong><%= it.clientName %></strong></div>
      <% } %>
      <div class="meta-card"><span>Guests</span><strong>Adults <%= it.pax?.adults || 0 %> · Children <%= it.pax?.children || 0 %></strong></div>
      <div class="meta-card"><span>Itinerary Total (USD)</span><strong><%= Number(it.totals?.grand || 0).toLocaleString('en-US') %></strong></div>
    </div>

    <section class="section">
      <h2>Trip Overview</h2>
      <p><%= it.overview || 'A tailored safari experience crafted for your travel dates, with comfortable stays and a balanced pace across the selected routes.' %></p>
    </section>

    <section class="section">
      <h2>Day-By-Day Journey</h2>
      <% formattedDays.forEach(function(day){ %>
        <article class="day-card">
          <h3>Day <%= day.index %>: <%= day.title %></h3>
          <div class="day-grid">
            <div class="day-text">
              <p><%= day.description || 'Details to be confirmed for this day.' %></p>
              <p class="accommodation">Accommodation: <%= day.accommodation %></p>
            </div>
            <div class="day-image">
              <% if (day.image) { %>
                <img src="<%= day.image %>" alt="<%= day.title %>" />
              <% } %>
            </div>
          </div>
        </article>
      <% }) %>
    </section>

    

    <section class="section">
      <h2>Price Inclusions & Exclusions</h2>
      <div class="list-grid">
        <div>
          <h3>Included</h3>
          <ul>
            <% inclusions.forEach(function(item){ %>
              <li><%= item %></li>
            <% }) %>
          </ul>
        </div>
        <div>
          <h3>Not Included</h3>
          <ul>
            <% exclusions.forEach(function(item){ %>
              <li><%= item %></li>
            <% }) %>
          </ul>
        </div>
      </div>
    </section>

    <section class="section">
      <h2>Service Extension</h2>
      <p>Need vehicle-and-guide only? Rate available on request (includes private Land Cruiser, professional guide, unlimited mileage, and bottled water).</p>
      <p>Welcome to Tanzania — your adventure starts here.</p>
    </section>

    <div class="print-actions">
      <button class="print-btn" onclick="window.print()">Print This Itinerary</button>
    </div>

    <footer class="company-info">
      <div class="company-grid">
        <div>Phone: <a href="tel:+255754042154">(+255) 754042154‬</a></div>
        <div>Email: <a href="mailto:info@tanzaniaadventures.co.tz">info@tanzaniaadventures.co.tz</a></div>
        <div>Address: P. O. Box 8786, Moshi, Kilimanjaro, Tanzania</div>
      </div>
    </footer>
  </div>
</body>
</html>
