<section class="py-4">
  <div class="d-flex align-items-center justify-content-between mb-3">
    <h1 class="h5 mb-0">Admin • Edit Accommodation</h1>
    <a class="btn btn-sm btn-outline-secondary" href="/admin/accommodations">Back</a>
  </div>
  <div class="row g-3">
    <div class="col-lg-6">
      <div class="card sb-card"><div class="card-body">
        <form action="/admin/accommodations/<%= accommodation._id %>/edit" method="post" class="sb-form">
          <div class="mb-2"><label class="form-label" for="accomodation_name">Accommodation Name</label><input id="accomodation_name" name="accomodation_name" class="form-control" value="<%= accommodation.accomodation_name %>" required></div>
          <div class="mb-2"><label class="form-label" for="place">Place</label>
            <input id="place" name="place" class="form-control" placeholder="e.g., Arusha" value="<%= accommodation.place %>" required>
          </div>
          <div class="form-check mb-3">
            <input class="form-check-input" type="checkbox" id="isLuxury" name="isLuxury" <%= accommodation.isLuxury ? 'checked' : '' %>>
            <label class="form-check-label" for="isLuxury">Luxury Accommodation</label>
          </div>
          <div class="d-flex justify-content-end gap-2">
            <a class="btn btn-outline-secondary" href="/admin/accommodations">Cancel</a>
            <button class="btn btn-primary" type="submit">Save Changes</button>
          </div>
        </form>
      </div></div>
    </div>
  </div>
</section>
