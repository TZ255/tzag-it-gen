<section class="py-4">
  <div class="card sb-card">
    <div class="card-body p-4">
      <div class="d-flex align-items-center mb-2">
        <span class="sb-hero-icon sb-hero-primary me-2"><i class="fa-solid fa-gauge"></i></span>
        <h1 class="h5 mb-0">Welcome<% if (user && user.name) { %>, <%= user.name %><% } %></h1>
      </div>
      <p class="text-muted mb-4">Create and manage itineraries for Tanzania Adventures Group.</p>
      <div class="d-flex gap-2">
        <a class="btn btn-primary" href="/dashboard/itineraries/new"><i class="fa-solid fa-plus me-1"></i> New Itinerary</a>
        <a class="btn btn-outline-secondary" href="/dashboard/itineraries"><i class="fa-solid fa-route me-1"></i> View Itineraries</a>
      </div>
    </div>
  </div>
</section>
