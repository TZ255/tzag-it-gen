const express = require('express');
const router = express.Router();

// Placeholder API router for itinerary system (extend as needed)
router.get('/ping', (_req, res) => res.json({ ok: true }));

module.exports = router;
